from setuptools import setup, find_packages

setup(
    name="robustness_of_vqa_and_captioning_models",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your project dependencies here
    ],
)